# 北北桃台語活動行事曆：系統規格與交接

本版更新於 2026-09-18，以實際程式與來源核查結果取代舊版「全部來源自動抓取」「100% 有效」等未經證實描述。

## 1. 工作目的與範圍

協助民眾找到臺北市、新北市、桃園市可實際參加的台語活動。正確的單場日期、時間、地點、語言內容與報名來源優先於活動數量。前端為靜態 HTML，資料內嵌，不依賴執行時 API。

## 2. 資料與模組

- `data/verified_activities.json`：正式活動場次入口。
- `crawler/verified.py`：核實紀錄、時間、地區、費用、重複場次與來源內容檢查。
- `main.py`：載入資料，移除已結束場次，暫存完整產物後替換 HTML / ICS。
- `build_html.py`：前端模板；CSS / JavaScript 大括號須在 Python f-string 中寫成 `{{`、`}}`。
- `crawler/sources/google_workspace.py`：ICS 與 Google 日曆加入連結；**沒有 Calendar API 同步功能**。
- `data/audit/2026-09-18-legacy.json`：原 64 筆資料、程式來源、搜尋語句及不刊登理由。
- `SOURCE_AUDIT.md`：核查摘要、官方證據、限制與後續事項。
- `CRAWLER_AUDIT.md`：所有指定平台、場館、局處的爬蟲實況；`crawler/audit_endpoints.py` 提供售票搜尋端點診斷，不是爬蟲驗收或發布輸入。
- `crawler/collect.py`：159 個收集器的候選收集流程；`collection.py` 提供安全 HTTP、HTML、候選結構；`culture.py` 解析文化部場次，`social.py` 處理 Meta 游標。
- `data/source_registry.json`：150 個官方機構／團體與分區入口與主管機關映射；`COLLECTORS.md` 說明方法、上限與授權。
- `crawler/sources/`：已改為真實資料收集；不再回傳固定資料。舊 `fetch_activities()` 接口會明確拒絕未審核資料，請改用 `.collect(client)`。
- `crawler/sample_data.py` 及 `processor.py`：保留的歷史實作；正式建置／候選收集均不使用。
- `config.example.json`：現行候選收集設定；僅含公開搜尋條件和流量上限。全部 Python 程式只需標準庫。

## 3. 正式資料規則

`schema_version: 1`；`sources` 按來源 ID 建立索引；`activities` 每筆包含 `activity` 與 `verification`。

來源需包含 HTTPS 活動／報名專頁 `url`、`title`、`checked_at`（含 +08:00）、核查當時原始 HTML 的 `snapshot_sha256`，以及可重查的 `required_text`。SHA-256 是本次讀取指紋，不是活動真實性的自動證明；未把整份第三方 HTML 納入 Git。

`registration_url` 為選用欄位，必須是無帳密 HTTPS 網址。它用於前端獨立「報名／買票」按鈕；`source_url` 仍固定指向可重查的活動詳情證據，兩者不能互相覆蓋。

每筆活動需具備：

1. 穩定 `id`，不能使用 Python 隨機 hash。
2. 官方可支持的標題、主辦／活動計畫名稱、場館及北北桃城市。
3. `start_time` 與可取得的 `end_time`，完整 ISO-8601 `+08:00`。不使用抓取時間、貼文時間或任意兩小時作為活動時間。結束時間未公告時可設 null。
4. `source_url` 必須等於核實來源；不允許首頁或搜尋頁。
5. 費用三態：`true` 確認免費、`false` 確認收費、`null` 未公告。未知不得歸為免費或付費。
6. `verification.status=verified`、`language_evidence` 及 `confirmed_fields`。已核實欄位異動後必須重新核對，不得只機械複製欄位繞過審查。
7. 系列活動逐場拆分，不能把首場至末場的總期間當成連續活動。
8. 圖片必須有可靠來源及使用依據。使用者指定將台語站活動海報加入詳情；僅讀取活動專頁 img#myPic 的 HTTPS 原圖，保留來源網址、核對時間與頁面指紋，不猜測圖片網址、不使用導覽圖示。卡片維持精簡排版，詳情完整顯示海報並可點開原圖；沒有海報或載入失敗時隱藏圖片區。

人工核實是事實判讀步驟；程式只能驗證結構、一致性與必要字串，不能自動證明活動必然舉辦或永不異動。

## 4. 建置與失敗處理

`python3 main.py`：離線驗證已核實資料。`python3 main.py --check-sources`：額外連線讀取所有來源，驗證 HTTPS、HTTP 成功、未轉址至其他網域及必要文字存在。

任何檢查失敗都在產物替換前拋出錯誤；不得回退示範資料、默默略過壞來源或標記整體成功。空清單是合法結果，頁面需說明「目前没有已核實場次」，不是認定當地沒有活動。

來源抓取時間與核實時間分開：建置不改寫 `checked_at`。來源文字仍存在不保證沒有新增取消通知；接近活動日期仍需人工重查。新活動由候選收集器發現；依第23節的官方證據規則重新核實通過，或經人工核查後才寫入正式清單，不能把收集時間當成核實時間。

OPENTIX 官方 HTML 的場次選單由動態 API 提供，不能只檢查節目內文。已核實來源另保存 `opentix_sessions`、`api_snapshot_sha256` 與 `api_checked_at`；正式活動以 `opentix_session_id` 對應。每日重查會比對官方 API 的場次時間、地點、票價、狀態與異動通知；缺場次或欄位改變即阻擋發布，不自動接受新值。

2026-09-18 本輪正式清單由 8 增為 58 場，新增與暫緩原因見 `data/audit/2026-09-18-published-expansion.json`。測試中的初始 8 場固定樣本保存在 `tests/fixtures/verified_catalog_base.json`，避免日常擴充正式清單破壞基礎驗證測試。

## 5. 前端與日曆

- 顯示台灣時間；篩選支援地區、六類活動、來源、費用、文字。
- 詳情直接連結官方活動／報名頁，顯示人工核對日期。
- Google 搜尋只是輔助查找，不是資料證據或保證能報名。
- Google Calendar 以 UTC 起迄時間搭配 `ctz=Asia/Taipei`。
- ICS 使用 UTC 時間、CRLF、文字跳脫、UTF-8 每行不超過 75 octets，保留穩定 UID；未提供結束時間時不產生虛構 DTEND。
- 單場下載、篩選下載與訂閱檔使用同一套後端序列化結果。
- 已下載並手動匯入的舊活動不會由本網站自動刪除；這次只更正公開訂閱檔及網頁。
- PWA manifest 保留；未實作 service worker，不宣稱離線快取可用。

## 6. GitHub Actions 與部署

`main` push、`workflow_dispatch`、候選收集 `workflow_run: completed` 觸發：單元測試 → 取得新鮮可信候選報告 → 核實及去重 → 官方來源重查 → 建置 → 前端檢查 → 提交資料及產物 → `public/` 靜態檔案上傳 → Pages 部署。原 UTC 20:00 獨立發布排程已取消，改為每日 UTC 19:15 收集完成後串接。`public/` 僅包含 HTML、ICS、manifest、assets；不部署爬蟲、核查記錄、設定或測試。

新來源或網站改版造成檢查失敗時，需讀取原公告並修正檢查內容，不能關閉驗證。不要把字串檢查通過當成新增活動已完成事實核實。

## 7. 接續事項

- 逐步增加臺北及其他北北桃來源；不為各城市配額加入無證據場次。
- 已重建公開 API、RSS、機構網站與 Meta 授權讀取接口；執行結果與剩餘限制見 `COLLECTORS.md`、`data/audit/2026-09-18-collection-report.json`。
- 社群缺 App／帳號授權，尚待真實 API 驗收；動態網站、防護頁、圖片公告及未連出的舊活動仍可能需要人工或瀏覽器補查。
- 新增來源先寫入候選區，通過場次核實後才進入正式清單。
- 維護時先讀 README / SPEC / SOURCE_AUDIT、確認 Git 狀態，保留他人未提交修改。

## 8. 指定館舍、台語路與非場次資訊

2026-09-18 第二輪新增國臺圖與台語路合作故事 3 場、臺博館台語繪本親子共作 2 場，正式清單共 63 場（臺北 11、新北 10、桃園 42），另有 5 項導覽／展覽資訊。

`data/verified_resources.json` 與 `crawler/resources.py` 管理非單場資訊：台語語音、預約導覽、展期資訊。必須有語言證據、核對時間、來源 SHA-256 與 required_text；`--check-sources` 同樣重查內容，失敗不覆寫。含 expires_at 的展覽到期後排除；不產生 ICS、不計入場次數。正式 UI 提供獨立資訊區及頁首跳轉入口，資料經 HTML escape。

使用者人工判讀保存在 `data/manual_candidate_decisions.json`，只對候選 ID、來源、網址及標題完全相符的項目生效；來源身分改變時回到 pending，不沿用舊決定。非活動商品可永久排除；與台語文化相關但缺少單場台語證據者，可另列 `traditional_performance_resource`，介面明示語言依當日節目，不計入活動或 ICS。

臺博館2020年導覽公告明示為舊頁，目前可讀取不代表真人導覽時間已確定。國臺圖故事每月日期採公告明列值，不能從「第3或4週」自行展開；系列總述11:00–12:00與單場11:00–11:50差異已向使用者顯示，採單場時間。臺博館活動需購票入館，is_free=false，說明活動本身免費。

第二輪當時收集入口共50個／registry41個；北美館改用官網實際 JSON 活動API，其餘指定館舍方法與限制詳見COLLECTORS.md。候選與正式刊登持續分離，相關推薦文字不可作為該展覽的台語證據。

通用網站收集器預設每站最多30頁（原8頁），包含入口、列表及詳情；可用 `--website-pages` 覆寫。候選收集排程時限為90分鐘，維持3個來源並行及既有請求間隔。頁數上限不代表最近三週完整涵蓋；達上限仍標 partial，`coverage_complete` 保持 false。


## 9. 各區圖書館與活動中心

新增54個圖書館分區來源、54個區公所活動中心公告來源，對應臺北12、新北29、桃園13區。registry共149入口；另含台語站等專用收集器後，全部收集器共159個。每個入口預設最多30頁，可用 `public_libraries`（59入口，含原有5個）及 `community_centers`（54入口）群組執行，每日候選收集排程自動納入。

`crawler/library_sources.py` 直接讀官方分館清單與實際分頁：臺北閱讀網53個分館／閱覽室列表分配至12區，沿同一列表的數字頁碼翻頁；新北依官方area代碼查該區所有館別，依表單及CSRF續頁；桃園送出完整Filter[0]至Filter[4]查詢、以CurrentPage表單翻頁，並逐筆比對data-area防止篩選失效。桃園區另含總館。行政區是收集範圍，不直接當作活動實際地點。列表至少預留所有初始分館入口，最多約一半頁數用於翻頁，其餘讀活動詳情，優先台語關鍵字；未讀完明確列partial。

活動中心範圍為區公所官網公開的里民、市民、社區、區民中心公告；沿活動、課程、研習及中心相關連結收集。場地租借／收費規則不是可參加活動，不納入候選。此設計不宣稱各里私有社團、未公開課表或每一活動中心的社群已全數涵蓋。來源名錄與端點驗收見 DISTRICT_SOURCES.md。

新北圖書館若HTTP502或其他連線失敗，保留failed，不用舊快照假裝本輪取得資料。列表重複、行政區篩選被忽略、頁面格式異動均作為錯誤；候選仍須人工核實後才可發布。


## 10. 各區候選核實刊登（2026-09-18）

本輪14筆候選新增18個場次及4項資源，現為81場（臺北27、新北10、桃園44）及9項資訊。逐筆紀錄位於 `data/audit/2026-09-18-district-publication-review.json`。

官方明示起訖日期、固定星期與時間的整期課程，可在確認總時數一致後拆成實際日期；永春課程16次共32小時，每次標示整期報名及影印費自付，不能當作16個獨立可報名活動，也不能因免學費而標示全免。

非場次類型增加 `reading_resource`，呈現臺語閱讀推廣；`exhibition_resource` 標籤為「台語相關展覽」，避免一般書展被誤稱為語音導覽。日期範圍但沒有固定時刻的活動以資源卡呈現，沒有日曆事件；日期到期界線使用結束日的次日零時，並非推定館方營業時間。全部資源仍須通過相同官方來源檢查。


## 11. Threads 指定帳號與回覆收集

`data/threads_accounts.json` 是實際追蹤名單，包含 `chhut_goa_kong_tai_gi`、`taigiloo`、`lesecondfloor`、`lekhiantang`。Facebook 不再列入預設收集器。Threads 收集器逐帳號、逐關鍵字呼叫官方 Keyword Search，以 `author_username` 精確限制帳號，拒絕回應中帳號不符、轉貼或回覆型結果；找到貼文後再讀 conversation 的可見回覆。

原始 `threads.json` 留在收集執行器；後續只接收 `threads_review.json` 去識別快照，內容限原帳號公開貼文短摘錄、公開連結、連結是否由原帳號提供及回應雜湊。其他回覆者身份及文字、Graph API 網址與 token 不得進入 artifact。分級判讀先排除已刊登官方連結，再將原帳號提供的 OPENTIX 連結轉為單場候選並套用第23節完整核實；原帳號提供且 HTTPS 主機名完全符合 Google Forms 或 Linktree 正式網域的報名連結標記為可信自動接手，不進人工 Issue。其餘不能完整核實者依缺連結、作者不明、不支援平台或驗證失敗分類為 pending，由部署工作更新一張固定 Threads GitHub Issue。未設定 token 逐帳號標 `needs_configuration`；不能解讀為帳號沒有活動。詳見 COLLECTORS.md。


## 11. 李江却基金會系列場次核實

基金會官方Feed候選需經文章正文重查與逐場核實。`foundation_details`預設30，與`feed_pages`分別計算；先處理可能仍有未來日期的文章，已過期或未解析文章不直接丟棄。正文僅取post-body，不混入側欄；失敗保留原候選並標partial。輸出review_queue及逐個session_hints，活動年份未明列時保持null，發文年份僅供排序提示。報名純文字URL和錨點均保存；Google Forms另列registration_links，不能排除content_links中其他平台。

日期線索不是核實場次，尤其系列共用時間、地點變動、線上課程、截止日與活動日期需人工判讀。9/18補核唸歌9/20、10/17與台文寫作9/19、10/18共4個免費場次，正式資料85場、9項非場次資訊；原8/29唸歌場次已過期。核實證據、先前漏刊原因與執行上限見foundation-review審查檔。

## 12. 週曆與城市配色（2026-09-18）

月曆改為週一至週日的週曆，提供上週、下週、本週導覽；起始週、今天及場次分組均依 Asia/Taipei，週次運算使用 UTC 日期避免裝置時區和夏令時間影響。每日完整列出符合現有篩選的活動，依開始時間排序，標題換行且清單無高度上限；同一天超過十場也不折疊。桌面七欄，1000px 以下逐日排列，768px 以下活動單欄。週標題顯示完整起訖日期，並顯示本週與每日場次數及空結果說明。

城市使用固定藍／綠／橘色（臺北／新北／桃園），週曆附圖例及城市文字；探索卡片、時間排程與詳情城市標籤一致。週曆活動按鈕支援鍵盤開啟詳情，保留原官方來源及日曆匯出功能。正式場次、核實證據與非場次資訊無異動。

前端回歸涵蓋週末14場完整列出、排序、城市篩選與配色、HTML文字跳脫、跨月跨年及閏日、臺北日期、本週導覽與空週；既有Python、官方來源重查及ICS下載檢查仍為發布門檻。

## 13. 月份複選與總館入口（2026-09-18）

前端以全部正式場次的Asia/Taipei開始日期產生YYYY-MM月份選項，按年月排序，不受其他篩選隱藏選項。selectedMonth為空表示全部；一次只能選一個YYYY-MM，改選即取代上一個，再與城市、類別、來源、費用及文字篩選取交集。重按同月維持選取，「攏選」清除。按鈕以aria-pressed呈現選取狀態，桌面與手機共用可水平捲動的列；維持按鈕DOM以保留鍵盤焦點。卡片、清單、週曆、計數及篩選後ICS使用同一組結果，非場次資訊不受活動月份篩選。若選月後目前週沒有符合活動，定位至篩選後首場活動所在週；沒有符合活動時定位至所選月份首日。週曆仍一次顯示一週。

新增臺北總館專用LibraryListingCrawler（tpml_main）；含後續台語站專用來源後，總數159收集器／150 registry，public_libraries群組60入口；排程預設全部來源會自動包含它。新北總館沿板橋區area=220、不限branch；桃園總館沿typl_district_2、area_codes包含1。限量驗收與各館證據見main-libraries-check；HTTP502、partial與未核實候選均不代表全館沒有活動或完整覆蓋。

## 14. 活動卡片精簡（2026-09-18）

無核實圖片的活動不建立封面或「台語活動・官方公告」佔位區塊。卡片直接顯示城市配色、類別、來源、日期時間、完整可換行標題、兩行摘要、可換行場地及費用；主辦完整資訊仍保留於活動詳情。僅真實cover_image存在時顯示圖片。月份、週曆、官方連結與ICS規則維持不變。

## 15. 簡潔台文介面與來源篩選（2026-09-18）

正式套用經預覽調整的灰白／深灰版面。來源使用單一下拉選單，從當次正式活動 source_platform 去重產生並標示場次數；選項值使用原始平台名稱，卡片可用 Accupass、市立圖書館、OPENTIX 短名顯示。來源、城市、月份聯集、類型、費用與搜尋共同取交集，卡片、清單、週曆與 ICS 共用 getFilteredActivities。未知費用仍不歸入免費或付費。

來源、種類及費用三個下拉選單，在桌面與手機都固定為同一列等寬三欄，標籤位於選單上方；三者的全部選項皆顯示「攏總」。長來源名稱不改變欄寬，完整名稱保留於原生下拉選項。

月份的全部選項亦簡稱「攏總」。來源、種類、費用標籤與選取文字皆置中；選單使用對稱內距，字級與月份按鈕統一為 13px，桌面及手機一致。

卡片採六列 subgrid，讓同列卡片的來源資訊、日期、完整標題、兩行簡介、地點與費用列對齊。場地文字向下對齊，分隔線上方留 8px；長標題與地點完整換行。週曆採按日分組的卡片，沒有場次的日期仍保留在七日日期列，有活動的日子完整展開，不設十場等顯示上限。圖片僅於活動詳情使用已核實的 cover_image。

卡片緊湊調整：內距 18px，標題下方 8px、簡介下方 12px；取消標題、簡介及場地的最低高度，僅由同列最長內容決定共用欄位高度。保留兩行簡介、完整標題／地點與場地下方 8px 的分隔線距離，活動及週曆共用此版面。

data/ui_taigi.json 與 data/ui_price_taigi.json 為編輯性台文翻譯，非核實來源：完整原始描述及所費文字作為 key，僅精確匹配時提供 description_taigi 與 price_info_taigi，不得把舊譯套用至已修改的官方文字。原始 title、venue、description、price_info 與 ICS 內容保留供核實；活動詳情顯示台文活動紹介及所費說明。資源摘要同樣以原始描述精確匹配翻譯。

assets/site.css 於建置時內嵌，使用系統字型而不載入遠端字型。main.py 在全部驗證成功後，產出 index.html、taigi_activities.ics 和相同內容的 taigi-activities-standalone.html；單檔副本為本機產物且加入 gitignore。GitHub Pages 繼續透過現有工作流程發布，未改變官方來源檢查或已核實資料門檻。

## 16. 預設深色與星期用字（2026-09-18）

HTML 初始 data-theme=dark，首次繪製前只在明確儲存 light 偏好時套用淺色，避免亮色閃爍。initTheme 與無效／受限 localStorage 均回退 dark；手動切換後保留偏好。PWA 啟動背景同步深色。日期介面統一「拜一、拜二、拜三、拜四、拜五、拜六、禮拜」；卡片、清單、週曆、詳情時間及分享時間共用臺灣時間語意，清單分組以 Asia/Taipei 日期鍵及 UTC 日期算術處理。台文摘要內的星期同步調整；官方描述、標題與 ICS 原文保留。

## 17. 手機 App 式操作（2026-09-19）

700px 以下使用精簡固定頁首與固定底部分頁列，直接切換活動、清單、週曆及日曆設定。隱藏桌面宣傳區及重複的檢視切換列，保留搜尋、城市和可橫向捲動的月份複選；資料收錄說明亦可從設定閱讀。底部導覽與面板包含手機安全區留白，主要操作至少44px。

手機顯示「佗位」與「幾月」標籤，兩列標籤欄皆30px，第一個「攏總」按鈕皆52px且左右對齊。城市及月份按鈕依aria-pressed呈現選取反白：深色模式為淺底深字、淺色模式為深底淺字；選中的城市按鈕取消內層彩色底以保持整顆反白，城市篩選名稱不加彩色底，活動卡片仍保留城市底色。城市單選、月份複選及「攏總」重設行為維持不變。

桌面與手機共用同一套選取底色規則，深色模式為白底黑字、淺色模式為黑底白字；手機未選取按鈕為透明底。桌面兩列標籤固定42px，移除月份捲動區左右內距，兩列「攏總」皆52px並左右對齊。

手機三個城市與年月選項統一為80×44px，兩列「攏總」仍為52×44px。標籤固定在左側，選項空間不足時可水平捲動，不壓縮按鈕寬高或造成整頁橫向溢出。

城市與月份選項停用原生按鈕外觀，使用獨立選取色：深色模式明確為純白底黑字，淺色模式為純黑底白字；文字填色跟隨文字顏色。瀏覽器回歸須從無儲存偏好的首次深色載入開始，檢查預選「攏總」、連續點選城市／月份及重新整理後的選取底色，不能只在切換主題後檢查。

手機活動卡片的日期與時間採`--text-muted`及400一般字重，與上方城市／來源及下方簡介一致；保留13px字級及原有日期格式，桌面字重不變。

來源、種類、費用由按鈕開啟底部面板；三個選單共用原有DOM及篩選狀態，仍同列等寬、置中、13px。選取立即更新結果和條件數，「攏總」僅清除這三項，保留城市、月份及搜尋。關閉面板或切回桌面寬度時，選單返回原位置；桌面仍直接顯示完整篩選列。

電腦版（701px以上）使用「來源［選單］　種類［選單］　費用［選單］」同一行排列，標籤在各自選單左側且垂直置中。選單各112px，標籤至選單間距8px，三組之間16px，整列靠左對齊上方篩選區。手機底部面板仍維持標籤在上、選單在下的等分三欄。

活動詳情及設定使用原生dialog，手機從底部展開，桌面置中。背景不可操作、焦點限制於面板，支援關閉按鈕、背景點擊與Escape；關閉後恢復觸發位置及頁面捲動。面板提供可見關閉按鈕，不宣稱支援滑動關閉。卡片保留來源、完整標題、兩行台文簡介、完整地點及活動詳情。資料來源、核實流程及ICS內容不變。

`tests/test_mobile_browser.cjs` 為選用的Playwright瀏覽器回歸，檢查320／390／700px、面板、篩選、底部分頁、焦點與捲動還原、深淺偏好及桌面切換；以NODE_PATH指定Playwright模組位置，必要時以BROWSER_EXECUTABLE指定隔離啟動的Chrome。沿用既有Python及Node測試，未新增爬蟲、排程或發布操作。


## 18. 日期顯示格式（2026-09-19）

活動卡片、清單日期群組、週曆每日標題、詳情及分享文字統一採 `YYYY∙MM∙DD 拜六`，月份、日期補零，星期不加括號，時間另接24小時制，例如`2026∙09∙19 拜六 10:00`。週曆起訖日期同樣使用圓點格式；月份篩選及篩選摘要使用`YYYY∙MM`，例如`2026∙09`。週曆當日日期不加底線，保留「今仔日」文字識別。日期及星期均依Asia/Taipei計算，不改動原始活動日期、官方說明或ICS。


## 19. 城市底色標籤（2026-09-19）

臺北、新北、桃園以藍、綠、橘的圓角底色區分，取代原有小圓點，套用於活動卡片、清單、週曆圖例及詳情。臺北藍與新北綠調亮、調淺，桃園底色不變；深淺模式各有對應底色，保留原有文字顏色、字級及字重。上方「佗位」三個城市名稱皆不加彩色底，選中的按鈕仍維持整顆反白；不改變活動資料與篩選行為。

## 20. 台語文字校對（2026-09-19）

檢查所有介面提示及78組台文簡介對照，依辭典詞義和語境調整27組簡介。學習認識的情境用「認捌」，略具能力用「略仔會曉」，每週三寫明「逐禮拜的拜三」；補明月份複選、空結果與操作指引。原詞合理變體不一律判錯。官方內容、對照索引鍵與全部數字資訊保持不變；校對範圍、取捨與實際查證來源見 `data/audit/2026-09-19-taigi-copy-review.md`。

使用者指定介面及台文簡介中的「費用」統一改為「所費」（sóo-huì）。含桌面標籤、手機篩選入口與面板、未公告提示和自撰說明；官方原文、對照索引鍵及資料欄位不變。

## 21. iPhone 單場日曆連結（2026-09-19）

活動詳情的 Apple 日曆入口改為普通連結，直接開啟該場的 HTTP(S) `.ics` 檔案，不使用 `blob:`、`download` 屬性或模擬點擊下載。建置以相同 ICS 匯出器生成 `calendar-events/<活動ID的SHA256>.ics`，每檔只有一個 VEVENT，日期、UID、地點、官方說明和換行折行規則皆與原全站 ICS 相同。正式網站使用相對網址，本機單檔 HTML 使用正式網站的 HTTPS 連結，因此此入口需要網路。

單場檔案在所有核實與建置完成後才更換，移除不再收錄的舊檔；來源檢查失敗保留舊產物。目錄為忽略版控的建置產物，GitHub Pages 工作流程必須一起上傳。既有依篩選下載整份 ICS 的功能不變。測試檢查逐檔與 HTML 對應、僅含目標活動、來源失敗不覆蓋、舊檔清除與真正連結；發布後確認 HTTP 200 及 `Content-Type: text/calendar`。WebKit 測試只能確認連結與檔案交付，無法代替實機 iPhone Calendar 的最終加入確認。

依使用者要求移除網站兩步驟說明與單場 ICS 備註中的預覽提示，按鈕恢復「加到 Apple 日曆 (.ics)」。單場 ICS 與全站 ICS 使用相同活動內容；連結使用 v=plain-notes-2 以更新先前含提示的檔案快取。iPhone 原生匯入流程仍由系統決定。

iPhone 的 LINE 內建瀏覽器不直接交接 `.ics` 時，網站辨識 LINE user agent，將 Apple 日曆入口改成帶 `activity` 與 `openExternalBrowser=1` 的正式網站網址，並顯示「右上角 ⋯ → 用預設瀏覽器開啟」提示。外部瀏覽器載入後依 `activity` 自動重開同一場活動詳情，再提供原本的單場 HTTPS ICS。一般 Safari 與其他瀏覽器仍直接開單場 ICS。

## 22. 活動名稱場次台語標示（2026-09-19）

依使用者指定，只翻譯活動名稱括號內獨立的場次標籤：上午場→早起場、下午場→下晝場、第N日→第N工；1至99的阿拉伯數字改為漢字，因此第1日／第2日顯示第一工／第二工。支援全半形括號及數字，保留括號外的正式名稱、作詞／作曲／台語場等其他括號內容與日期。

原始核實資料的 title 保留，建置另產生 title_taigi；卡片、清單、週曆、詳情、分享及加入 Google／Apple 日曆的標題一致採用新標示，搜尋同時接受原名和台語標示。ICS UID、時間、地點、原文說明不變。統一轉換由 crawler/presentation.py 提供，不以修改官方來源紀錄來通過核實檢查。

## 23. 候選核實與自動發布串接（2026-09-19）

使用者授權在網站更新前核實候選，通過者自動加入且不得重複已刊登活動。`crawler/review.py` 執行保守、可重現的官方資料核對；這是明列規則的自動核實，不宣稱人工逐頁判讀或涵蓋所有公告類型。每筆決策保存在 `data/audit/latest-candidate-review.json`。

自動通過適配器支援 OPENTIX、受限的 ACCUPASS 單一／系列場次與台語站。OPENTIX 只接受固定官方 event URL；重讀官方 API 及 HTML，核對節目 ID、單場 ID、臺北時間起迄、北北桃場館地址、主辦、票面價格、節目狀態3及場次狀態0，明確語言／發音標示必須出現在該場館注意事項或節目正文，並在 HTML 可見內容中相符。ACCUPASS 單場適配器接受 JSON-LD 為單一場次、標題或內文明列「台語場」、完整起迄不超過12小時、北北桃地點、主辦、EventScheduled 狀態及正文明列免費報名者；發布後每日仍重讀相同證據。台語站由使用者指定為全部皆是台語活動，故 `trusted_language_source=true` 直接滿足語言證據；仍須重新讀詳情頁，比對標題、北北桃地址、場地、明確場次及純文字／來源欄的報名網址。系列按規則逐場拆分，只有開始時間時不虛構結束時間；過期排除，疑似跨來源重複擋下，缺完整日期或開始時間維持候選。其他來源也逐筆列出原因，仍須人工核實，不能從發文日期或系列期間猜場次。

自動通過的資料另存 `verification.mode=official_rules_v1` 與逐欄快照，來源保留語言宣告位置、頁面/API 指紋及各自重查時間。後續 `--check-sources` 同時比對原有場次欄位與自動來源的語言、節目狀態、異動公告；失敗不發布。既有人工 source 與活動不覆寫，自動新增來源使用獨立 `auto_op_` 或 `auto_acc_` ID。自動活動保留官方語言摘錄作核查原文；ACCUPASS簡介依第25節取官方介紹，其他適配器仍以事實模板顯示，不猜劇情。

去重以既有全部歷史紀錄與本輪已通過結果為基準：官方 session ID優先，其次官方頁＋起迄時間，再比對跨站正規化標題／城市／地點／時間。相同城市時間但標題或場地疑似重複時保留 pending。同一官方系列的不同日期可分別通過；同一報告反覆執行不重複新增。過期活動只從輸出排除，核實歷史仍保留。

`fetch_review_candidates.py` 僅下載同倉庫、main、collect.yml、schedule/workflow_dispatch、最近36小時完成的 success/failure 工作。workflow_run 使用觸發的同一 run，push／手動發布選最近一輪完成工作；不使用其他分支／PR artifact，不執行 artifact 內容。檔名／大小／JSON／來源數量一致性與新鮮度不符即停止；原始社群授權內容不下載，Threads僅下載上述去識別快照。個別來源有 errors 不影響其他來源的核實，但收集結果仍保留 failure／partial 狀態。

所有正式檢查通過才提交核實資料、翻譯對照、審查紀錄及 HTML/ICS。來源重查涵蓋本次建置仍未結束的活動；已結束活動繼續保留不可變的核實歷史，但其已下架或改版的舊詳情頁不阻擋今日活動發布。提交推送失敗會停止部署，沒有 continue-on-error 或強制覆蓋 main。候選核實報告保留 Git 及14天 artifact，全文候選不進 Git／Pages。Python 測試保存9/18的85場固定樣本；當前正式清單另通過結構、來源與日曆一致性檢查，沒有85場數量上限。

台語站缺少明確開始時間時，收集與核實皆會沿官方公告的 docs.google.com、forms.gle、ppt.cc、reurl.cc 連結讀公開 Google Forms。最終網址須為 HTTPS docs.google.com 的 viewform；表單標題須有唯一完整年月日，與公告月日、公告明列年份及場地相符。僅將「場次」題目下的「第N場」選項拆成活動，報名組合不另算場次；不提交表單或讀取填答。表單文字與選項的同一起始時間若出現不同結束時間，保留確定的開始時間、end_time=null，詳情註明結束時間待確認，ICS 不虛構 DTEND。來源快照保存表單目的網址、標題、指紋、場次選項及結束時間各種記載；後續發布必須重新核對，變更或抓取失敗會停止發布。首次收集失敗仍保留公告及失敗原因供後續處理。海報OCR及較通用的多層報名頁補時規則已依第26節擴充。


## 24. 官方活動海報（2026-09-19）

`crawler/posters.py` 共用圖片擷取規則。ACCUPASS 使用該活動 Event JSON-LD 的 image；李江却使用 post-body 內圖片及發布者明列的原圖連結；圖書館支援活動ID綁定主圖、公告正文、lazy data-src 與具海報標示的圖片附件；文化部報名頁使用活動專屬輪播，北美館使用 divImgs 及公開 PlayImg 欄位，桃園美術館使用公開資料集公告正文圖片。僅 HTTPS、無帳密的圖片網址；保留 query、正確編碼中文與空白，不猜圖片路徑或改寫縮圖尺寸。排除標誌、圖示、表情、推薦活動、社群分享與頁尾。每場沿用 cover_image 表示官方主要活動圖片，維持既有詳情完整顯示、原圖連結、無圖／載入失敗隱藏行為。

收集器將圖片保存於候選欄位，ACCUPASS 自動核實時以重新取得的官方圖片為準。每次 `main.py --check-sources` 共用來源核實已取得的HTML，全部活動來源通過後再將最新圖片套用至輸出 Activity，並在 raw_metadata.poster_evidence 保存網址、來源、時間與指紋；不增加請求、不改活動核實欄位，不據圖片自動修改日期。官方頁移除圖片時當輪輸出也移除；來源驗證失敗仍禁止覆蓋網站。這一輪另外將58場的21張圖片及 poster_evidence 回填正式清單，供離線建置使用；日後線上建置取得的圖片不自動改寫歷史核實資料。現有歷史場次測試只允許具來源證據的圖片補充，其餘欄位持續逐一比對。

圖片型式解析有明確範圍；未支援的動態版型、PDF海報及來源防護頁不猜測圖片。桃園美術館本輪首頁回HTTP428，因此只驗證既有公開資料集解析規則，不宣稱本輪成功抓取其最新海報。非場次資源卡片維持既有版型，不建立日曆活動。


## 25. ACCUPASS 系列逐場核實及簡介回退（2026-09-19）

`table_schedule` 展開 rowspan／colspan，僅核實有「日期」「時間」欄的完整表格。日期可為年月日、月日或斜線格式；缺年份時只能由官方 JSON-LD 期間得出唯一年份，核對星期、有效起迄及整份表格首末日期；跨年期間可支援，超過兩個年份差、重複起始時間、缺資料或不同場地／語言等附加欄均保留待核實。不能只挑解析成功的部分列直接發布。

核實重新讀官方頁，要求全程台語宣告、活動費用明列免費、JSON-LD無收費票種、共用北北桃場地、主辦及正常活動狀態。系列候選路由成穩定場次ID，每場仍依既有過期、跨來源去重及欄位比對規則處理；使用者排除決定優先。每場獨立的 auto_acc_*_series_* 來源保存完整場次表和該場事實；後續發布重查整份表格、語言、費用及單場資訊，異動則停止發布，不靜默接受改期。

ACCUPASS 原先取 Event.description 前三句且最多160字；此限制已由第30節的活動正文擷取取代。不得使用整頁導覽／廣告文字，也不以通用台文模板假裝翻譯。缺少對應台文時，卡片、清單、詳情統一顯示原文；詳情只有原文也沒有時才顯示待整理。當前《時間來的車》已另行撰寫有來源支持的完整介紹與台文，保留原活動ID及原人工核實欄位，來源追加場次表重查證據。核查紀錄見 data/audit/2026-09-19-accupass-series.json。


## 26. 台語站多層報名文字與最後手段OCR（2026-09-19）

`crawler/registration.py` 提供有界公開報名頁追蹤；`crawler/poster_ocr.py` 將圖片辨識與事實核實分開。`supplement_registration` 保留原呼叫介面，收集、逐場核實及發布重查共用相同順序：公告已有完整場次時結束；否則嘗試Google Forms、BeClass或ACCUPASS正文／場次，再在文字無法補足時嘗試海報。Google Forms舊「第N場」解析仍保留，另支援正文與其他日期／場次選項；只補公告缺的資訊，不默默變更已有起始時間。表單明確的結束時間衝突保留null，不進OCR挑答案。

公開連結限支援的HTTPS主機與活動／viewform路徑；短網址及Linktree最多再兩層、共6頁。跳轉在連線前檢查主機及路徑，禁止讀表單回覆路徑或轉到任意站點。不提交、不登入；只取活動正文／場次選項，排除輸入欄、非場次題目、側欄與截止時間。跨頁活動名稱片段、完整場地、公告日期或年份不符、星期不合、多個相符表單有不同時間、額外未完成連結均保留原因，不能任選一頁通過。成功來源保存每層網址、最終目的地、頁面指紋與場次文字。

OCR只讀台語站活動圖`img#myPic`的允許HTTPS圖檔，最大8MB；不解QR code、不以OCR結果自動開新網址。每個Client最多30張，單張Tesseract45秒；macOS使用Vision，Swift首次編譯最多240秒並重用模組快取。GitHub兩個工作安裝`tesseract-ocr`、`tesseract-ocr-chi-tra`、`tesseract-ocr-eng`，CLI輸出TSV，每行採最低詞信心值。macOS備援透過`scripts/poster_ocr.swift`讀同一原圖，不上傳第三方OCR服務。引擎不在／失敗／超限都有明確原因。

所有日期、年份與時間證據行信心值至少85/100，活動名稱與場地也須出現在高信心文字；這只是自動採用門檻，不代表正確率保證。年份須由海報或公告明列且唯一，公告補年時另存原文證據；不取今天或發文年；不修猜O/0等數字，不虛構結束時間。圖片或表單衝突不相互覆蓋。已在公告確定的開始時間可保留，未確認的結束時間維持null；OCR沒有通過時保留辨識嘗試及失敗原因供判讀。通過時保存圖片SHA-256、文字SHA-256、引擎、日期時間證據行及場次，後續發布重抓與重辨識；圖片／場次異動或強證據改變即停止發布。候選仍經過期、地區及去重檢查，沒有繞過既有核實與發布閘門。

OCR測試圖為程式繪製的中文日期時間fixture，不是外部活動海報；Linux CI執行真實Tesseract辨識測試。本機已對台語站9/20真實海報執行Vision，讀到開始時間但低信心／身分資訊不完整，正確不自動採用；同一活動由下一層公開表單補時，保持原先已核實兩場與第一場結束時間衝突。

## 27. 台語站活動內容與分場台文（2026-09-19）

- `parse_detail` 同時保存完整正文及清理後的 `official_description`；核實刊登保留實際內容，排除網址、加LINE等宣傳文字，不得用站台信任聲明替代介紹。
- 人工依來源編寫的台文存於 `data/directory_editorial.json`，以來源URL、正文指紋及精確場次對應；提供官方事實摘要 `description`、完整 `description_taigi`、獨立 `summary_taigi` 及 `source_quotes`。來源原文更新或其他場次不得沿用舊版本。
- 卡片／清單顯示短簡介；詳情完整呈現台文段落，保留參加方式與原有時間衝突提醒。其他來源既有顯示不變，ICS保留事實摘要而不使用卡片短句。
- 首批9篇公告28場完成台文；古錐ê講古的水母巴士與果子牛分別介紹，夾腳拖按日期保留故事題名，人權繪本早起場不可混入下晝場的書單。
- 沒有校訂台文的新內容會保留實際公告介紹，明示「簡介原文」，不以通用台文冒充翻譯。事實引文加入來源重查；新文案不授權變更活動的時間、地點或費用。

## 28. ACCUPASS 主來源與跨站合併（2026-09-19）

- 台語站詳情的報名連結若為合法 HTTPS ACCUPASS `/event/<id>`，以活動ID正規化網址，忽略分享參數；不同event ID／開始時間／城市不能互相合併。同時間存在多個主來源候選時不任選。
- `source_priority.py` 決定正式刊登優先序；`load_verified` 先驗證歷史紀錄，再排除台語站副本。只有主來源核實完成才可刊登；沒有匹配場次者保留原始紀錄，列待核實但不出現在HTML、ICS及單場日曆。
- `review` 重新讀台語站報名連結，已刊ACCUPASS場次列duplicate；未核實者直接讀ACCUPASS活動頁並送回其核實流程。主來源不可被舊的台語站副本擋成重複。直呼台語站核實也不得繞過主來源規則。
- 已完成的台語介紹可附加到主來源卡片／詳情，保留ACCUPASS原有參加資訊；日期、費用、地點、海報與公告連結仍以ACCUPASS紀錄為準。補充文案的台語站來源亦納入發布前重查。ICS保留主來源活動ID與正式描述。
- 首輪4場重複合併；12/19小旅行因主來源日期矛盾暫停刊登。歷史資料保留，後續核實主來源即可恢復該場，不需重建台語站副本。

## 29. 週曆日期按鈕（2026-09-19）

日期列改為七個原生button，以`aria-pressed`呈現選取，label含完整日期、星期與符合篩選的場次數。`selectedCalendarDay`僅控制週曆日區塊；同日再點取消、跨週清除，篩選導致週次改變時也會清除超出範圍的選取。選取的空日期必須顯示空場次提示，不能被原有隱藏空日CSS遮蔽；選定一天仍完整呈現當天全部活動。日期重繪後保留按鈕鍵盤焦點、不強制捲動。活動／清單及ICS匯出保留原有全域篩選行為。

## 30. ACCUPASS 講者、演出者與完整台文（2026-09-19）

爬蟲優先保留 ACCUPASS 專用 EventContent 活動正文，包含文末講師、演出名單與替演公告；不再截成前三句／160字。不認得版型或正文不唯一時，退回完整 Event.description，不使用包含導覽／推薦活動的整頁文字。此為原文擷取；新的未校訂內容仍明示原文，不冒充台語翻譯。

`data/accupass_editorial.json` 保存11個來源44筆場次的台語卡片簡介與完整介紹（本輪42場尚未結束），逐場綁定ID、來源、標題、起迄及原描述，保留抓取時間、快照指紋與原文引文。來源引文加入發布前重查，場次綁定異動或缺證據即停止建置。歷史正式欄位／ICS不因文案補充而改寫；此台文優先於台語站補充文案。卡片最多顯示四行，詳情完整顯示段落；講者姓名放在摘要前方，詳細內容保留分場書單、工作坊連續上課條件與演員替換公告。


## 31. 單月篩選與週曆精簡（2026-09-19）

頁首為「北北桃 • 講Tâigí」，主標為「講Tâigí • 真歡喜」，地區篩選標籤為「城市」。月份採單一狀態，卡片、清單、週曆和ICS共用篩選結果。週曆隱藏全域總場數與月份摘要，移除城市圖例與週摘要的「臺北時間」文字，保留每週／每日場次資訊、日期選擇及原有Asia/Taipei時間計算；切回活動或清單恢復全域摘要。城市與月份選單的全選按鈕為「攏選」，月份標籤為「月份」。手機底部導覽恢復原有無外框、選取項目圓角底色的樣式，保留選取狀態、鍵盤焦點、至少44px觸控區域與安全區間距。
